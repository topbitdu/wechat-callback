require 'spec_helper'

describe Wechat::Callback do
  it 'has a version number' do
    expect(Wechat::Callback::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
